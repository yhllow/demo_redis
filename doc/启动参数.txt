//防止服务器在不安全的状态下执行写命令
min-slaves-to-write 3//从服务器数量少于3个
min-slaves-max-lag 10//或者3个从服务器的延迟(lag)值都大于或等于10秒
lag：心跳检测，正常情况下1秒发送一次，该值是上次检测成功距离当前时间的秒差，所以该值一般为0或1。

//复制积压缓存区，默认为1m，是个固定大小的先进先出队列
//用于部分重同步的实现P204，当从服务和主服务器断开后，从服务器后来有连接上主服务器，此时复制积压缓存区的数据会发送给从服务器
//但如果这里的数据不够，则需要完整重同步
repl-backlog-size 1mb